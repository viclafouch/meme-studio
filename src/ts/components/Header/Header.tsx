import * as React from 'react'
import './header.scss'

function Header(): JSX.Element {
  return (
    <header className="Header">
      <h1>Meme Studio</h1>
    </header>
  )
}

export default Header
